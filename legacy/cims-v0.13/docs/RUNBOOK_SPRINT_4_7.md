# Local Runbook Sprint 4-7

1. `cp .env.example .env`
2. Terminal A: `npm run mock:provider`
3. Terminal B: `npm run api`
4. Terminal C: `npm run web`
5. Open `http://localhost:4173`
6. Run tests with `npm test`

Reset development data with `npm run reset:db`.
