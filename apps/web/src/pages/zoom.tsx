import { useQuery } from '@tanstack/react-query'; import { api } from '@/lib/api'; import { PageHeader } from '@/components/page-header'; import { Card,CardContent,CardDescription,CardHeader,CardTitle } from '@/components/ui/card'; import { Badge } from '@/components/ui/badge'; import { Button } from '@/components/ui/button';
export function ZoomPage(){const q=useQuery({queryKey:['zoom-status'],queryFn:()=>api<{mode:string;connected:boolean;provider_url:string;detail:unknown}>('/zoom/status')});return <><PageHeader title="Zoom Provider" description="Status adapter provider-neutral yang telah dipindahkan ke TypeScript." action={<Button variant="outline" onClick={()=>q.refetch()}>Refresh</Button>}/><Card><CardHeader><CardTitle>Connection</CardTitle><CardDescription>Live Zoom tetap memerlukan Server-to-Server OAuth dan secret manager.</CardDescription></CardHeader><CardContent className="space-y-4"><Badge variant={q.data?.connected?'success':'warning'}>{q.isLoading?'CHECKING':q.data?.connected?'CONNECTED':'NOT CONNECTED'}</Badge><pre className="overflow-auto rounded-lg bg-slate-950 p-4 text-xs text-blue-100">{JSON.stringify(q.data??{message:'Provider belum berjalan.'},null,2)}</pre></CardContent></Card></>}
